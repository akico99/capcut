# 영상 편집 프로젝트

- 편집 스타일·자막 레이아웃·제목 공식은 `STYLE_GUIDE.md` 가 기준. 컷편집/자막/제목 작업 지시를 받으면 먼저 읽는다.
- 파이프라인 사용법은 `SETUP.md`, 에이전트 호출 규약은 `AGENT_INTERFACE.md`.
- CapCut 드래프트 생성 시 `캡컷 작업시 주의사항.txt` 확인.
- 새 PC/세션에서 첫 컷편집 작업 전에는 `python check_env.py`로 설치 현황을 먼저 확인한다 (Python 3.10 전체 경로 사용).
